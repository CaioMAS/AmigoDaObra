/* 
    Note: Being more used to use typescript than javascript,
    I would use an enum holding the values. Since javascript
    doesn't support enums yet, I'm using an object.
*/

const inputValues = {
    propCimento: `propCimento`,
    propAreia: `propAreia`,
    propBrita: `propBrita`,
    volumeConcreto: `volumeConcreto`,
}

export { inputValues }